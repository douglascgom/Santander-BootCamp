/*
var jogador1 = 0;
var jogador2 = 0;
var placar;

//Operador Ternario
jogador1 != -1 && jogador2 != -1 ? console.log('jogadores sao validos'): console.log('jogadores invalidos')

    //Usando if
    if(jogador1 > 0 && jogador2 == 0 ){
        console.log('jogador 1 marcou ponto')
        placar = jogador1 > jogador2;
    } 
    
    //Usando else if
    else if (jogador2 > 0 && jogador1 == 0){
        console.log('jogador 2 marcou ponto')
        placar = jogador2 > jogador1;
    } 
    
    //usando else
    else {
        console.log('ninguem marcou ponto')
    }

    switch(placar){
        case placar = jogador1 > jogador2:
            console.log('Jogador 1 ganhou')
            break;
        case placar = jogador2 > jogador1:
            console.log('Jogador 2 ganhou')
            break;
        default:
            console.log('Empate')
    }
    */

    /*var array = ['valor1', 'valor2', 'valor3', 'valor4', 'valor5',];

    var object = { propriedade1: 'valor1', propriedade2: 'valor2', propriedade3: 'valor3',}
    */

    /*for - executa uma instrucao ate que ela seja falsa

    for(var indice = 0; indice < array.length; indice++){
        console.log(indice)
    }
    */

    /*for/in executa repeticao a partir de uma propriedade
     com array
    /*for(var i in array){
        console.log(i);
    }

    //com object
    for(i in object){
    console.log(i)
    }
    */

    /*for/of executa repeticao a partir de valor
    //com array
    for(i of array){
        console.log(i)
    }
    //com object
    for (i of object.propriedade1) {
    console.log(1);

    }
    */

   var a = 0;
   
   while(a < 10) {
       a++;
       console.log(a);

   } 